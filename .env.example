# 理财助手 · 环境变量模板
# cp .env.example .env  然后按需填值. 全部都是可选的.

# Claude API key — 早盘 LLM 简报使用. 二选一:
# 1. 已经装了 Claude Code CLI 并登录过 → 不需要填, 自动从 macOS Keychain 读 OAuth
# 2. 没装 Claude Code → 填一个 API key
# ANTHROPIC_API_KEY=sk-ant-xxx

# 扩展数据源(provider)插件 — 可选. 不填就只用自带的公开行情源, 一切照常.
# 协议与示例: docs/providers.md
# MARKET_PROVIDER=mypkg.myprovider:Provider
